from ALS_to_Mysql import *
user_id = 1000
mesage ='推薦系統'
# mesage = 'apple'
detail = ''
if mesage == '推薦系統':
    if show_recommendation(user_id):
        recommendation_user = show_recommendation(user_id)
        detail = recommendation_detail(recommendation_user)
        # 呈現的表單
    else:
        line_bot_api.reply_message(event.reply_token,TextSendMessage(text='請輸入商品名稱'))
        recommendation_item = show_recommendation_item(mesage) # mesage = 'apple'
        detail = recommendation_detail(recommendation_item)
        # 呈現的表單